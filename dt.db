a primeira tabela esta no arquivo conn.php, pq criei direto no php pro mysql.

a segunda tabela é essa:

// CREATE TABLE `crud_php`.`tasks` (
//     `id` INT NOT NULL AUTO_INCREMENT,
//     `title` VARCHAR(45) NULL,
//     `description` VARCHAR(150) NULL,
//     `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
//     PRIMARY KEY (`id`));
  

Boa aula man.
